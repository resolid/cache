{
  "name": "resolid-cache",
  "private": true,
  "description": "High-performance cache abstraction for Node.js and modern JavaScript runtimes.",
  "homepage": "https://www.resolid.tech",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/resolid/cache.git"
  },
  "type": "module",
  "scripts": {
    "format": "oxfmt",
    "lint": "pnpm run -r --if-present lint",
    "typecheck": "pnpm run -r --if-present typecheck",
    "test": "pnpm run -r --if-present test",
    "build": "pnpm run -r --if-present build"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.1",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@resolid/config": "^6.1.0",
    "@types/node": "^24.13.3",
    "@vitest/coverage-v8": "^4.1.10",
    "lefthook": "^2.1.10",
    "oxfmt": "^0.60.0",
    "oxlint": "^1.75.0",
    "oxlint-tsgolint": "^7.0.2001",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": "^22.22.0 || >=24"
  },
  "packageManager": "pnpm@11.15.1"
}
